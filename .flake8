[flake8]
max-line-length = 88      # match Black’s defaults
extend-ignore =
    E203,   # whitespace before ':' (conflicts with Black)
    W503,   # line break before binary operator (Black prefers after)
